# 个人网站项目 · 交接说明（先读这份）

> 这份文档是给 Claude Code 的项目起点说明。读完这份，再看 `CLAUDE.md`（仓库里的操作约定）和 `DEPLOY.md`（部署方案）。
> 站主会全程在场，任何拿不准的决策都直接问她，别自己脑补。

---

## 一、这是什么项目

给站主本人做一个**个人网站**，定位不只是求职作品集，而是一个长期的「我是谁」的入口。它同时承载两件事：

1. **展示**——关于我、工程项目、联系方式。
2. **写作**——一个博客。站主会在小红书等平台发内容，同时**在自己网站上重新写一份正典版本**，网站是内容真正的家，小红书是分发渠道之一。

技术上是一个 **Astro 静态站**，源码放 GitHub，内容以 Markdown 存在仓库里。日常发文章不靠站主手动敲 CMS，而是**她把内容交给 agent（Claude Code 或她的 OpenClaw / 小龙虾），由 agent 按约定同步进仓库**。所以本仓库既是网站源码，也是这套 agent 工作流的落地点。

---

## 二、已经定下来的决策（不要推翻，除非站主改主意）

- **框架**：Astro，纯静态输出。
- **主题**：海洋主题，浅色蓝调。已有一版原型（见第四节），以它为视觉/结构起点。
- **语言**：右上角 `中 / EN / 日 / DE` 四语切换。**目前只写中文**，英/日/德先留空、自动回退显示中文；以后逐条补翻译。中文内容要先打磨到位再翻。
- **结构**：**多页面**，不是一路下滑的单页。顶部导航栏：`首页 / 关于 / 项目 / 文章 / 联系`，每个是独立页面。文章多了以后，列表页做**分页**（第 1 页 / 第 2 页……），不要无限滚动瀑布流。
- **两类「作品」要分开**：`项目`（工程项目，如检索工具、冰箱系统、Context Copilot）和 `文章`（从小红书等平台搬来重写的内容）是两个独立板块，别混。前者画「会造东西的人」，后者画「会表达、有观点的人」。
- **源与部署**：源码在 GitHub，用 GitHub Actions 自动构建。**第一阶段只部署到 GitHub Pages**（免费、免备案、当天能上线）。火山引擎作为**以后可选的第二出口**（覆盖国内受众时再加，需 ICP 备案）。细节见 `DEPLOY.md`。
- **同步文章有一道草稿关卡**：agent 不直接推上线。新文章先以 `draft: true` 或独立分支/PR 存在，站主 review 后才发布。防止 agent 把排版搞歪直接翻车。

---

## 三、关于站主本人（供生成文案参考，有错请她纠正）

- 现于**九州大学**读信息科学方向硕士（预计 2027 年 3 月毕业）。
- 研究方向：英语教育场景下，大语言模型对**以东亚语言为母语的学习者**（"East Asian language-background learners"）所持有的「母语者中心主义（native-speakerism）」偏见。
  - ⚠️ **措辞硬性要求**：涉及研究对象时，一律用「以东亚语言为母语的学习者 / East Asian language-background learners」，**绝不写成「中文/普通话母语者」「Chinese/Mandarin speakers」**。
- 本科：北京航空航天大学，德语语言文学；有德国开姆尼茨工业大学交换经历。
- 语言：中文母语、日语商务级、英语专业级、德语日常会话。
- 持 AWS Certified AI Practitioner。
- 运营一个约 60 人的女性 AI 学习社群。
- 兴趣：弹吉他、英超、研究中日德三国高端制造业竞争格局（最后一项也是她的职业坐标）。
- 常驻福冈。GitHub：`Okayamasenko`。

**文案口吻**：直接、不煽情、有观点。项目叙事强调「用 AI 自主开发」（是她主导用 AI 造的，不是「AI 帮她做的」）。别堆形容词，别写成简历腔。

### 三个项目的现成叙事（原型里已有中文，可直接沿用/打磨）

1. **论文检索工具**：为自己做了 7 个 CLI 论文检索工具 → 发现同实验室的人卡在 Python 安装 → 重构成打开浏览器就能用的 HTML 单文件版 → 实验室 4 人采用，文献阅读和组会汇报效率提升。核心是「观察到真实障碍，然后把它做没」这个循环。
2. **智能冰箱管理系统**：ESP32-CAM 拍摄 + Claude Vision API 识别食材 + Telegram Bot 交互，结合膳食指南给建议，渐进式搭建中。
3. **Context Copilot**：回答「我现在该做什么」的个人副驾，把散落的任务/笔记/行为信号整合成可行动的上下文；也是她 10 周 AI 学习计划（prompt engineering、RAG、agent 开发）的载体。

---

## 四、当前进度：已有一版原型

网页版聊天里已经做出一版海洋主题的原型（单文件 Astro，`src/pages/index.astro`），站主手上有 `senko-site.zip`。它是**单页**的，用来定视觉方向（海面渐深背景、Hero 波浪线、四语切换、藍/纸白/潮色配色）。

**你的任务不是照搬它，而是以它的视觉语言为基础，重构成上面第二节说的「多页面 + 博客 + 分页」结构。** 可以把原型里的配色、字体、波浪、语言切换逻辑抽成共享的 layout / 组件复用。

---

## 五、建议的推进顺序

1. **搭骨架**：初始化 Astro 工程，建多页面路由（首页/关于/项目/文章/联系）+ 共享 layout + 顶部导航 + 四语切换。把原型的海洋样式迁进来。
2. **接内容层**：配置 Astro content collections（`src/content/posts/`），定义文章 frontmatter schema（见 `CLAUDE.md`），做文章列表页 + 分页 + 单篇文章页。
3. **通 CI**：配 GitHub Actions，push 即构建并部署到 GitHub Pages（见 `DEPLOY.md`）。跑通「改一个字 → push → 自动上线」。
4. **落地同步工作流**：确认 `CLAUDE.md` 里的内容约定可用，走一遍「给一篇小红书内容 → agent 生成草稿 → review → 发布」。
5. **（以后）** 补英/日/德翻译；需要覆盖国内时加火山引擎第二出口。

先跟站主确认从第 1 步开始，不要一口气全做完再给她看——每完成一步给她过一眼。
